info:
  name: Delete item
  type: http
  seq: 9
  tags: [auth-local]

http:
  method: DELETE
  url: "{{baseUrl}}/wp-json/smooth/v1/items/1"
  params:
    - name: id
      value: "1"
      type: path
      description: Item row id
  auth:
    type: basic
    username: "{{wpUsername}}"
    password: "{{appPassword}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.data.deleted
      operator: eq
      value: "true"

settings:
  encodeUrl: true

docs: |-
  ## Purpose
  Delete an item row. Its modifiers (publish and draft) cascade.

  ## Auth
  Capability `smooth_manage_menus` (401 without credentials, 403 without cap). Local-only: needs app-password credentials.

  ## Parameters
  | Name | In | Required | Description |
  | ---- | -- | -------- | ----------- |
  | id | path | yes | Item row id |

  ## Success example
  ```json
  {"data": {"deleted": true, "id": 1}}
  ```

  ## Error cases
  - `401` — missing/invalid credentials
  - `403` — authenticated but lacking the cap
  - `404` — unknown id (`smooth_menu_item_not_found`)

  ## Notes / Changelog
  - 2026-09-19: created alongside SMO-144
