import { __ } from '@wordpress/i18n';
import { PlaceholderPage } from '../components/PlaceholderPage';

export const MenuItems = () => (
	<PlaceholderPage
		title={ __( 'Menu Items', 'smooth-restaurant' ) }
		description={ __( 'Manage your menu here.', 'smooth-restaurant' ) }
	/>
);
